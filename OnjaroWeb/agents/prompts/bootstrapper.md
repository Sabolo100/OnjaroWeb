Create the initial version of the web application described in CLAUDE.md.

## CLAUDE.md (Project Constitution):
{claude_md}

## Your task:
1. Choose the appropriate tech stack (use what CLAUDE.md specifies, or default to Next.js + TypeScript + Tailwind CSS)
2. Create the initial project structure
3. Implement a minimal but working homepage that reflects the product goal
4. Set up basic routing if needed
5. Make sure the app builds and lints successfully
6. Create a simple, clean landing page that matches the product goal and UX principles

## Important:
- Run `npm install` after creating package.json
- Run `npm run build` to verify everything works
- Keep the initial version MINIMAL - just the homepage/main screen
- Style it properly according to the UX principles in CLAUDE.md

The goal is a working seed app that the evolution system can then improve over time.